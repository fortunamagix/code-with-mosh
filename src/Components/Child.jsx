// eslint-disable-next-line react/prop-types
function Child({name}) {
    return (
        <div>
            <h2>Child Component</h2>
            <p>Hello, {name}!</p>
        </div>
    )
}

export default Child
